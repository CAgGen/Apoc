fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.92
httpx>=0.27
crawl4ai>=0.6
trafilatura>=2.0
pydantic>=2.7
python-multipart>=0.0.9
langgraph>=0.2
pypdf>=4.0
langfuse>=3.0.0
# langfuse's LangChain integration needs the full langchain package, not just
# langchain-core — without it the tracing callback silently no-ops.
langchain>=1,<2
