server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — let React Router-style client routing work on reload
    location / {
        try_files $uri $uri/ /index.html;
    }
}
